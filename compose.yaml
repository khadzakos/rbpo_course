services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=sqlite:///./chore_tracker.db
    volumes:
      - .:/app
    working_dir: /app
